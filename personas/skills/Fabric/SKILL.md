---
name: Fabric
description: Intelligent prompt pattern system with 260+ specialized patterns for content analysis, extraction, and transformation. Each pattern is also available as a `/pattern-name` slash command (underscores become hyphens). USE WHEN user says 'use fabric', 'fabric pattern', 'run fabric', 'update fabric', 'update patterns', 'sync fabric', 'extract wisdom', 'summarize with fabric', 'create threat model', 'analyze with fabric', OR any request to apply Fabric patterns to content.
---

## Customization

**Before executing, check for user customizations at:**
`~/.claude/skills/SNAP/USER/SKILLCUSTOMIZATIONS/Fabric/`

If this directory exists, load and apply any PREFERENCES.md, configurations, or resources found there. These override default behavior. If the directory does not exist, proceed with skill defaults.

## Voice Notification

**When executing a workflow, do BOTH:**

1. **Send voice notification**:
   ```bash
   curl -s -X POST http://localhost:8888/notify \
     -H "Content-Type: application/json" \
     -d '{"message": "Running the WORKFLOWNAME workflow in the Fabric skill to ACTION"}' \
     > /dev/null 2>&1 &
   ```

2. **Output text notification**:
   ```
   Running the **WorkflowName** workflow in the **Fabric** skill to ACTION...
   ```

**Full documentation:** `~/.claude/skills/SNAP/SYSTEM/THENOTIFICATIONSYSTEM.md`

# Fabric

Intelligent prompt pattern system providing 260+ specialized patterns for content analysis, extraction, summarization, threat modeling, and transformation. Every pattern is available as a native `/pattern-name` slash command — no Fabric CLI required.

**Patterns Location:** `~/.claude/skills/Fabric/Patterns/`
**Slash Commands:** `~/.claude/commands/` (auto-generated by `Tools/GenerateCommands.py`)

---

## Workflow Routing

| Workflow | Trigger | File |
|----------|---------|------|
| **ExecutePattern** | "use fabric", "run pattern", "apply pattern", "extract wisdom", "summarize", "analyze with fabric" | `Workflows/ExecutePattern.md` |
| **SyncPatterns** | "update fabric", "update patterns", "sync fabric", "pull patterns" | `Workflows/SyncPatterns.md` |

---

## Examples

**Example 1: Extract wisdom from content**
```
User: "Use fabric to extract wisdom from this article"
-> Invokes ExecutePattern workflow
-> Selects extract_wisdom pattern
-> Reads Patterns/extract_wisdom/system.md
-> Applies pattern to content
-> Returns structured IDEAS, INSIGHTS, QUOTES, etc.
```

**Example 2: Update patterns**
```
User: "Update fabric patterns"
-> Invokes SyncPatterns workflow
-> Git sparse-checkout from upstream fabric repository
-> Syncs patterns to Patterns/ directory
-> Regenerates slash commands
-> Reports pattern count delta
```

**Example 3: Create threat model**
```
User: "Use fabric to create a threat model for this API"
-> Invokes ExecutePattern workflow
-> Selects create_threat_model pattern
-> Applies STRIDE methodology
-> Returns structured threat analysis
```

---

## Quick Reference

### Pattern Execution (Native - No CLI Required)

Instead of calling `fabric -p pattern_name`, SNAP executes patterns natively:
1. Reads `Patterns/{pattern_name}/system.md`
2. Applies pattern instructions directly as prompt
3. Returns results without external CLI calls

### Slash Commands

Each pattern is available as a `/pattern-name` slash command (underscores become hyphens):
- `/extract-wisdom` runs the `extract_wisdom` pattern
- `/create-threat-model` runs the `create_threat_model` pattern
- `/patterns` lists all available patterns, `/patterns [search]` filters them

### Most Common Patterns

| Intent | Pattern | Description |
|--------|---------|-------------|
| Extract insights | `extract_wisdom` | IDEAS, INSIGHTS, QUOTES, HABITS |
| Summarize | `summarize` | General summary |
| 5-sentence summary | `create_5_sentence_summary` | Ultra-concise |
| Threat model | `create_threat_model` | Security threat analysis |
| Analyze claims | `analyze_claims` | Fact-check claims |
| Improve writing | `improve_writing` | Writing enhancement |
| Code review | `review_code` | Code analysis |
| Main idea | `extract_main_idea` | Core message extraction |

### Full Pattern Catalog

See `PatternCatalog.md` for complete list of 260+ patterns organized by category.

---

## Native Pattern Execution

**How it works:**

```
User Request → Pattern Selection → Read system.md → Apply → Return Results
```

**Pattern Structure:**
```
Patterns/
├── extract_wisdom/
│   └── system.md       # The prompt instructions
├── summarize/
│   └── system.md
├── create_threat_model/
│   └── system.md
└── ...260+ patterns
```

Each pattern's `system.md` contains the full prompt that defines:
- IDENTITY (who the AI should be)
- PURPOSE (what to accomplish)
- STEPS (how to process input)
- OUTPUT (structured format)

---

## Pattern Categories

| Category | Count | Examples |
|----------|-------|----------|
| **Extraction** | 30+ | extract_wisdom, extract_insights, extract_main_idea |
| **Summarization** | 20+ | summarize, create_5_sentence_summary, youtube_summary |
| **Analysis** | 35+ | analyze_claims, analyze_code, analyze_threat_report |
| **Creation** | 50+ | create_threat_model, create_prd, create_mermaid_visualization |
| **Improvement** | 10+ | improve_writing, improve_prompt, review_code |
| **Security** | 15 | create_stride_threat_model, create_sigma_rules, analyze_malware |
| **Rating** | 8 | rate_content, judge_output, rate_ai_response |

---

## Integration

### Feeds Into
- **Research** - Fabric patterns enhance research analysis
- **Blogging** - Content summarization and improvement
- **Security** - Threat modeling and analysis

### Uses
- **yt-dlp** - For YouTube transcript extraction (`~/gopath/bin/yt-dlp`)
- **WebFetch** - For URL content fetching (native Claude Code tool)
- **Native execution** - Direct pattern application via slash commands or Fabric skill

---

## File Organization

| Path | Purpose |
|------|---------|
| `~/.claude/skills/Fabric/Patterns/` | Local pattern storage (260+) |
| `~/.claude/skills/Fabric/PatternCatalog.md` | Full pattern documentation |
| `~/.claude/skills/Fabric/Workflows/` | Execution workflows |
| `~/.claude/skills/Fabric/Tools/` | Generator utilities |
| `~/.claude/commands/` | Auto-generated slash commands (one per pattern) |

---

## Changelog

### 2026-02-17
- Every pattern now available as native `/pattern-name` slash command (260 commands)
- Removed Fabric CLI dependency entirely — yt-dlp for YouTube, WebFetch for URLs
- New `Tools/GenerateCommands.py` generates command files from pattern directories
- Replaced `UpdatePatterns` workflow with `SyncPatterns` (git sparse-checkout, no CLI)
- Added `/patterns` master command for listing/searching all patterns
- Pattern count: 237 -> 262

### 2026-01-18
- Initial skill creation (extracted from SNAP/Tools/fabric)
- Native pattern execution (no CLI dependency for most patterns)
- Two workflows: ExecutePattern, UpdatePatterns
- 260+ patterns organized by category
- SNAP Pack ready structure
